function testArr2(n) {
    let testArr = []; //O(1);
    for(let i = 0; i < n.length; i++) {
      hiArr[i] = 'hi'; //O(n)
    }
    return hiArr; O(1);
  }
  
  testArr2(n);
  // space complexity of this function is O(n) - Linear;