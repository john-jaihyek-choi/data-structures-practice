const testArr = [0, 1, 2, 3, 4, 5];

function testFunction (arr) {
  console.log(testArr[0]);
  console.log(testArr[4]);
}

// This is a constant time operation because this operation independent on number of input;
